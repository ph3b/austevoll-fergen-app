version: '1.0'
steps:
  build_image:
    type: build
    description: Building the image...
    working_directory: ./app
    dockerfile: ./app/Dockerfile
    image-name: ph3b/austevoll
    tag: ${{CF_REVISION}} # ${{CF_BRANCH}}
